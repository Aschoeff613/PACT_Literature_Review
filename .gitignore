# --- secrets -------------------------------------------------------------
.env
.env.*

# --- python --------------------------------------------------------------
__pycache__/
*.pyc
.claude/

# --- os / archives -------------------------------------------------------
.DS_Store
*.zip

# --- generated data ------------------------------------------------------
# Full text is bulky and licence-encumbered; never commit it.
data/fulltext/
data/*fulltext*/
data/*.log

# Steps 1-3 (see docs/STEPS_1_3.md): commit the provenance, log, counts and
# manifest files so a run can be audited. Do NOT commit the PMID lists --
# they are large and fully reproducible from the query in 01_search_log.csv.
# 11_sample_500.csv IS committed deliberately, so collaborators get the
# screening sample from `git pull` rather than a side channel.
data/01_pmids_*.csv
data/11_draw_pmids.csv
data/03_screening.csv

# Step 4 blinding: these hold the AI decisions for the 300 validation papers.
# The repo is public, so they stay out of git until all six reviewers have
# exported their decisions. Commit them after step 5.
data/04_validation_master.csv
data/04_warmup.csv
data/03_screening.csv
